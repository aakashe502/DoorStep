<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hadIt.doorstep">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".ManageOtp" />
        <activity android:name=".MobileVarificationActivity"></activity>

        <activity android:name=".ui.Admin.ViewCategory"></activity>
        <activity android:name=".ui.Admin.AddGrocery" />
        <activity android:name=".CategoryClass" />

        <activity android:name=".SearchActivity" />
        <activity
            android:name=".HomePage"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".SaveDetailsToFirestore"
            android:parentActivityName=".LoginActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
    </application>

</manifest>